# W01 plan — Repository and Devflow bootstrap

1. Create the Python 3.12 package and CLI.
2. Add domain, adapter, harvesting, storage, audit, and export layers.
3. Add deterministic fixtures and tests.
4. Add canonical task state, required stage documents, and validators.
5. Add product, state, secret, and post-merge workflows.
6. Validate compilation, tests, state consistency, and secret boundary.

Validation: repository-full deterministic gate passes locally or in CI; Draft PR is open.
