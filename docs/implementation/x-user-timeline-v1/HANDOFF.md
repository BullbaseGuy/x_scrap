# Handoff

Resume from `W02_plan.md` on `feature/x-user-timeline-v1`.

Before changing product behavior:

1. run the repository-full gate;
2. inspect `task_state.yaml` and Task #1;
3. keep X-specific fixes inside `src/x_scrap/adapters/`;
4. do not add real cookies or captured account data to tests;
5. keep Codex disabled.

The first live credential requirement belongs to W09, not earlier deterministic stages.
