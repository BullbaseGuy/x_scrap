name: Temporary W05 Branch Snapshot

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/temp-w05-snapshot.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Package current branch
        shell: bash
        run: |
          set -euo pipefail
          tar --exclude=.git --exclude=.venv -czf /tmp/w05-branch-snapshot.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: x-scrap-w05-branch-snapshot
          path: /tmp/w05-branch-snapshot.tar.gz
          retention-days: 1
          if-no-files-found: error
